require 'spec_helper'

feature "Editing projects" do
  before do
    # visit show page then click edit project link
  end
  scenario "updating a project"
  scenario "updating a project with invalid attributes is bad"
end
